# Итоговый проект по курсу «Инженерия Искусственного Интеллекта»

В этой папке находится итоговый мини-проект по курсу.  
Проект должен демонстрировать применение методов и инструментов инженерии ИИ: работу с данными, модели, пайплайны, сервис, эксперименты и (по возможности) воспроизводимость.

Заполните данный файл под ваш конкретный проект.

---

## 1. Паспорт проекта

- **Название проекта:** `Почасовой прогноз спроса на использование прокатных велосипедов в Нью-Йорке`
- **Автор:** `Атаманчук Алексей Викторович`
- **Группа:** `КВБО-01-22`
- **Контакт:** `@chockol00`

- **Краткое описание (2-4 предложения):**  
  `Проект строит почасовой прогноз спроса на велопрокат по открытым данным использования Citi Bike (2023–2024) и погоде соответствующим им данным погоды из Open-Meteo. Сравниваются сезонный naive baseline и модель градиентного бустинга на табличных признаках (календарь, лаги, погода). Финальная модель упакована в REST API: в котором можно получить прогноз на сутки вперёд, передав историю использования и соответствующую погоду.`

---

## 2. Структура проекта

Проект организован в следующей структуре:

- `requirements.txt` – зависимости проекта (библиотеки Python, необходимые для запуска).
- `report.md` – отчёт по проекту (постановка задачи, данные, эксперименты, результаты).
- `self-checklist.md` – чеклист самопроверки проекта перед сдачей.
- `Dockerfile` – контейнер с API (копируются `artifacts/`, `data/processed/`, `src/`, `configs/`).
- `architecture.png` – схема пайплайна (для `report.md`).
- `notebooks/` – экспериментальные ноутбуки:
  - EDA, прототипы моделей, отдельные эксперименты.
- `src/` – основной код проекта:
  - модули для работы с данными;
  - обучение моделей;
  - запуск сервиса/скриптов.
- `data/` – демонстрационные/учебные данные:
  - **только открытые, синтетические или обезличенные данные;**
  - при необходимости – небольшие выборки для быстрого запуска.
- `configs/` – конфигурационные файлы:
  - `.yaml`, `.json`, `.env.example` и т.п.
- `tests/` – тесты (юнит-тесты, простые проверки).
- `artifacts/` – сохранённые модели, отчёты, результаты экспериментов.

Если структура проекта отличается от базовой, опишите это здесь.

---

## 3. Требования и установка

### 3.1. Требования

- Python `>= 3.10` (Docker - 3.11).
- Установленные системные зависимости (если есть: например, `git`, `make` и т.п.).

### 3.2. Установка окружения

Примерный сценарий (можно адаптировать под ваш проект):

```bash
# Перейти в папку проекта
cd project

# Создать виртуальное окружение (опционально, но рекомендуется)
python -m venv .venv

# Активировать окружение:
# Windows:
.venv\Scripts\activate
# Linux / macOS:
source .venv/bin/activate

# Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

Если у проекта есть какие-то особенности установки (дополнительные шаги, внешние сервисы и т.п.), опишите их здесь.

Дополнительно нужен доступ в интернет для первичной загрузки Citi Bike (10–20 Гб в `data/raw`), либо использование уже сформированного `hourly_demand`.
---

## 4. Как запустить проект

Опишите, как именно воспроизвести основные сценарии использования проекта.

Примеры (заполните под свой случай):

### 4.1. Запуск обучения модели

```bash
# из папки project/
# после активации venv
python -m src.data.download_citibike --list # план загрузки (доступность ресурсов для скачивания по промежутку из конфига)
python -m src.data.download_and_build # если ещё нет hourly_demand - скачивание и билд по данным
python -m src.models.train
python -m src.analysis.ablation_study
python -m src.analysis.export_report_figures # PNG в artifacts/figures/
```

```bash
# если только для тестов/отладки, можно запустить билд на синтетике
python -m src.data.build_dataset --synthetic
python -m src.models.train
```

### 4.2. Запуск сервиса (API/веб-интерфейс)

```bash
# из папки project/
python -m uvicorn src.service.app:app --host 0.0.0.0 --port 8000
```

Или, если используется Docker:

```bash
# из папки project/
docker build -t citibike-demand .
docker run -p 8000:8000 citibike-demand
```

Опишите:

- на каком порту поднимается сервис;
- какие эндпоинты есть (минимум – 1-2 ключевых);
- как протестировать работоспособность (например, запрос через `curl` или браузер).

Поднимается на порту 8000.  
Эндпоинты: `GET /health` - статус; `GET /model-info` - имя модели, метрики на test; `POST /predict` - прогноз на `horizon` (24).  
Проверка: Swagger http://localhost:8000/docs или прямые запросы curl к http://localhost:8000/health; для полного прогона - `python -m src.service.client` (тело для `/predict` из `artifacts/sample_request.json`).

---

## 5. Данные

Кратко опишите используемые данные:

- источник (открытый датасет, синтетика, собственная генерация и т.п.);
- где лежат файлы:

  - `data/` – небольшие выборки, необходимые для демонстрации;
  - **не** храните в репозитории большие файлы и конфиденциальные данные;
- если данные нужно скачать отдельно – опишите шаги или добавьте скрипт.

Источники: поездки Citi Bike - открытый датасет поездок (https://citibikenyc.com/system-data), прямой доступ по (https://s3.amazonaws.com/tripdata/index.html); почасовая погода, NYC из Open-Meteo (https://open-meteo.com/en/docs/historical-weather-api). Период, использованный в работе - 2023-01-01 : 2024-12-31. Хранение локально - в data/raw, не попадает в git (в .gitignore). Для установки:
``` 
python -m src.data.download*
```
В data/processed - уже агрегированные записи по часам (`hourly_demand.parquet`) на используемый промежуток: 17к почасовых записей. Формируется на основе источников по заданному периоду билодом через `build_dataset.py` либо `download_and_build.py`.

---

## 6. Тесты (если есть)

Если вы добавляете тесты:

- опишите, какие есть тесты (модульные, e2e, sanity-checkи);
- приведите команду запуска.

9 тестов: парсинг URL Citi Bike, признаки без утечки, smoke train на синтетике, API /health и /predict.

Запуск:

```bash
cd project
source .venv/bin/activate
pytest tests
```

Тесты:

`test_citibike_urls.py` - Парсинг имён ZIP на S3  
`test_citibike_urls.py` - План загрузки 2023/2024  
`test_features.py` - Лаги без утечки из будущего  
`test_features.py` - 17 признаков  
`test_features.py` - Строки после warmup  
`test_model_smoke.py` - `train` на синтетике `metrics.json`  
`test_service.py` - `/health`, модель загружена  
`test_service.py` - `/predict` на sample payload  
`test_service.py` - 400 при history < 168 ч  

---

## 7. Демонстрация на защите

Опишите, **как именно вы планируете показывать проект на защите**:

- что вы запускаете (ноутбук, сервис, отдельный скрипт);
- какие 1-2 ключевых сценария продемонстрируете;
- какие метрики или результаты покажете (графики, таблицы, ответы сервиса).

На защите я:

1) Буду использовать существующий data/processed/hourly_demand.parquet по 2023-2024. 
2) Покажу результаты экспериментов в ноутбуках: 01_eda.ipynb - сезонность спроса, профиль по часам, связь с осадками, 03_model_comparison.ipynb - сравнение моделей на test, 04_ablation_weather.ipynb - вклад погоды, лагов и календаря.
3) Покажу ключевые метрики из artifacts/metrics.json на test - последние 14 дней 2024
4) Запущу сервис, продемонстрирую его работоспособность через Swagger + работоспособность /predict с использованием sample_request.json


---

## 8. Ограничения и дальнейшая работа

Кратко опишите:

- текущие ограничения проекта (что не успели реализовать, какие есть упрощения);
- направления, в которых проект можно развивать дальше.

Пока прогноз по всей системе проката. Все данные объединяются в почасовые записи для упрощения. В рамках дальнейшего развития проекта рассматривается возможность учёта локальных прогнозов погоды (районы/станции), а также идея рассматривать сами районы (группы станций), как минимальные объекты исследования. 
Улучшение API при реализации связи с актуальными данными - не передача истории при обращении к API, а использование последних данных и отправка запросов к API погоды на бэкенде.
Слабая свзяь по важным событиям: ремонта станций, промо и т.п.
Из-за агрегата по всему городу погода возможно смешивается по районам (пики не выражены)

---

## 9. Оценка проекта

Итоговая оценка за проект выставляется по пятибалльной шкале (2-5).

Ориентиры для оценки:

- **2** – проект не принят:
  - не выполняются минимальные требования (сервис не запускается, отсутствует ключевой функционал);
  - грубые нарушения требований курса;
  - явный плагиат или формальная имитация работы.
- **3** – проект принят, но реализован на базовом уровне:
  - минимальный функционал есть;
  - по чеклисту `self-checklist.md` выполнено **меньше 5** пунктов;
  - эксперименты и документация слабо проработаны.
- **4** – хороший, рабочий проект:
  - сервис запускается по `README.md`, `/predict` использует реальную модель;
  - есть данные, EDA и эксперименты с метриками;
  - структура кода и конфигураций в целом адекватна;
  - по чеклисту выполнено **не менее 5** пунктов.
- **5** – сильный, хорошо проработанный проект:
  - аккуратно реализован сервис и пайплайн;
  - проведены осмысленные эксперименты и обоснован выбор финальной модели;
  - есть базовая наблюдаемость и работа с конфигами/секретами;
  - документация позволяет быстро понять и воспроизвести решение;
  - по чеклисту выполнено **не менее 9** пунктов.

Чеклист `self-checklist.md` служит для самопроверки студента и как подсказка при проверке.  
Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта чеклиста;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, продвинутая ML-часть и т.п.);
- соблюдение требований курса и дедлайнов.

---
